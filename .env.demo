# 演示环境配置（Netlify 部署专用：npm run build:demo）
# 仅用于给他人预览，全部数据来自前端 Mock，无需后端。
# 此文件不影响正式构建（npm run build 走 .env.production）。

# 接口前缀保持 /api，由浏览器端 mock 拦截
VITE_API_BASE_URL=/api

# 启用前端 Mock（演示核心开关）
VITE_USE_MOCK=true
